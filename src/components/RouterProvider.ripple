import type { Component } from 'ripple';

/**
 * RouterProvider component.
 *
 * @param props - The router provider props.
 * @param props.$children - The children of the router provider.
 */
export component RouterProvider(props: { $children: Component }) {
    const { $children } = props;
    <$children/>
}
